# Secrets
.env

# Database / runtime data
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log

# Local media source files — NOT committed. Only the Telegram file_ids are kept
# (in data/catalog.json), so the audio/PDF binaries never go into the repo.
/media/
/ЛУХУФ. СКОРБИ КЕРБЕЛЫ/
*.mp3
*.wav
*.m4a
*.m4b
