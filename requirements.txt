# --- Core ---
aiogram~=3.15            # Telegram Bot framework (async). Bot API 7.x+ rich entities.
SQLAlchemy[asyncio]~=2.0 # ORM, async. SQLite now -> Postgres later without code changes.
aiosqlite~=0.20          # async SQLite driver
pydantic-settings~=2.5   # typed configuration from .env

# --- Optional / production ---
uvloop~=0.21 ; sys_platform != "win32"   # faster event loop on Linux VPS (not on Windows)
